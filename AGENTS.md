# AI Gateway Contribution Rules

本文件适用于整个 `ai-gateway` 仓库及其所有子目录。

## 项目文档

代码、配置、依赖、数据库结构、API、测试或目录结构发生变化时，必须检查以下文档并保持其对当前实现的描述准确：

- `ARCHITECTURE.md`：模块职责、依赖方向、关键调用链、数据关系、运行与部署边界发生变化时同步更新。
- `CODEBASE.md`：目录、文件、类型、接口、关键业务职责、路由、模型、配置或测试范围发生变化时同步更新。

文档应描述当前代码状态，不写成逐次变更日志。纯格式或拼写修改不需要改正文，但仍应确认文档没有因此失真。

## 提交前检查

- 运行与修改范围匹配的测试。
- 执行 `git diff --check`。
- 最终说明列出同步更新的文档；若无需更新正文，说明已检查且无语义影响。
